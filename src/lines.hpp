#pragma once
#include "inc.hpp"

class Lines {
public:
	static void DrawLongs(const float px, const float py, const float angle, const float len);

	static void DrawCircle(const float px, const float py, const float r, const int n);
};